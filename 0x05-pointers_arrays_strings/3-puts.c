#include "main.h"
#include <stdio.h>
#include <string.h>
/**
 * _puts-"This pogram reset the variable with pointers"
 * @str: Le pointer
 * Return: "Always 0 (Success)"
 */

void _puts(char *str)
{
	puts(str);
}
