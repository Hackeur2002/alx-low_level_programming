#include "main.h"
#include <stdio.h>
#include <string.h>
/**
 * print_rev-"This pogram reset the variable with pointers"
 * @s: Le pointer
 * Return: "Always 0 (Success)"
 */

void print_rev(char *s)
{
	
	puts(str);
}
