#include "main.h"
#include <stdio.h>

/**
 * _strlen-"This pogram reset the variable with pointers"
 * @s: Le pointer
 * return: lenght of a pointer
 */

void swap_int(char *s)
{
	return (strlen(*s));
}
