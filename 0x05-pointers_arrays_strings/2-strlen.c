#include "main.h"
#include <stdio.h>
#include <string.h>
/**
 * _strlen-"This pogram reset the variable with pointers"
 * @s: Le pointer
 * return: "Always lenght of string s"
 */

int _strlen(char *s)
{
	return (strlen(s));
}
