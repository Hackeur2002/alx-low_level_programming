#include "main.h"
#include <stdio.h>
#include <string.h>
/**
 * _strlen-"This pogram reset the variable with pointers"
 * @s: Le pointer
 * return: strlen(s)
 */

int _strlen(char *s)
{
	return (strlen(s));
}
