#include "main.h"
#include <stdio.h>
#include <ctype.h>
#include <stdlib.h>
/**
 * mul-"this program multiplie deux nombres"
 * @a: the number to check
 * @b: the second number
 * Return: The result of the multiplication
 * */

int mul(int a, int b)
{
	return (a * b);
}
