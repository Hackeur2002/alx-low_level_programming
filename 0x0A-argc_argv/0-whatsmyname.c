#include <stdio.h>
#include "main.h"
/**
 * main - prints the name
 * @argc: number of argument
 * @argv: un tablea
 * Return: 0 Always
 */
int main(int argc __attribute__((unused)), char *argv[])
{
	printf("%s\n", argv[0]);
	return (0);
}
